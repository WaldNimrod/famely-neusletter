# Famely Neuslettr — Environment Variables
# Copy to .env and fill in real values

# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# FTP (nimrod.bio / upress.co.il)
FTP_HOST=ftp.upress.co.il
FTP_USER=
FTP_PASS=
FTP_PATH=/newsletter

# WhatsApp (Twilio)
WHATSAPP_PROVIDER=twilio
TWILIO_SID=
TWILIO_TOKEN=
TWILIO_FROM=whatsapp:+...

# Email (SMTP fallback)
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=famely@nimrod.bio

# Webhook
WEBHOOK_SECRET=
